#! /usr/bin/env bash

# Use environment variable to set the config directory
# to my dotfiles repo, easier and more automatic than
# setting up symlinks or includes
KITTY_CONFIG_DIRECTORY='${HOME}/dotfiles/Kitty'